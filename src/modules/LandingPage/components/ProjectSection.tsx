import React from 'react';
import { SECTION_ID } from '@/routes';

const ProjectSection = () => {
  return (
    <div id={SECTION_ID.PROJECT} className="min-h-screen">
      ProjectSection
    </div>
  );
};

export default ProjectSection;
