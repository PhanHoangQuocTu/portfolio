import React from 'react';
import { SECTION_ID } from '@/routes';

const EducationSection = () => {
  return (
    <div id={SECTION_ID.EDUCATION} className="min-h-screen">
      EducationSection
    </div>
  );
};

export default EducationSection;
